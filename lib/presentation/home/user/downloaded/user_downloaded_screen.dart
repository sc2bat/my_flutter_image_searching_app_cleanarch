import 'package:flutter/material.dart';

class UserDownloadedScreen extends StatelessWidget {
  const UserDownloadedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('downloaded'),
      ),
    );
  }
}
